export enum Layout {
  default = "default",
  auth = "auth",
}

export enum RouteName {
  login = "login",
  main = "main",
  trash = "trash",
}

export enum RouteTo {
  login = "/login",
  main = "/",
  trash = "/trash",
}

export enum LocalStorageName {
  token = "token",
}
