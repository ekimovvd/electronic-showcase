export enum Layout {
  default = "default",
  auth = "auth",
}

export enum RouteName {
  login = "login",
  main = "main",
}

export enum RouteTo {
  login = "/login",
  main = "/",
}

export enum LocalStorageName {
  token = "token",
}
